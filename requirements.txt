# Modelo local NL→MQL (Ollama — llama3.2 por defecto)
ollama>=0.3.0

# Base de datos
pymongo>=4.7.0
certifi>=2024.0.0

# Web
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0

# Utilidades
python-dotenv>=1.0.0
pydantic>=2.7.0
# (bson y json_util — usados por scripts/load_sample_datasets.py — vienen incluidos en pymongo)

# Tests
pytest>=8.2.0
httpx>=0.27.0   # FastAPI TestClient (tests/test_app.py)
